# Streamlit secrets: NEVER commit the real secrets.toml
# In Docker deployment, these values come from environment variables instead.
# This file exists only for local-dev-outside-Docker reference.

[connections.postgresql]
host = "localhost"
port = 5432
database = "command_center"
username = "portfolio_reader"
password = "CHANGE_ME"
